-- [[ SECURITY & ANTI-RESET INITIALIZATION ]] --
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TweenService = game:GetService("TweenService")
local LocalPlayer = Players.LocalPlayer

-- Memastikan UI tidak hilang saat berpindah dari lobi ke dalam game (In-Game Spawn)
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "RCAHUB_CyberCore_V4"
ScreenGui.ResetOnSpawn = false -- Kunci utama agar tidak hilang saat mati/pindah map
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

local function attachUI()
    local pGui = LocalPlayer:ToDataModel():GetService("Players").LocalPlayer:WaitForChild("PlayerGui", 5)
    if pGui then 
        ScreenGui.Parent = pGui 
    end
end
attachUI()
LocalPlayer.CharacterAdded:Connect(attachUI) -- Re-attach otomatis jika struktur interface dibersihkan server

local Features = {
    HealAura = false,
    DetectKiller = false,
    SilentAim = false
}

local CORRECT_KEY = "Cheva"

-- Helper Style Cyber Hitam-Putih
local function applyCyberStyle(instance, bgTransparency)
    instance.BackgroundColor3 = Color3.fromRGB(5, 5, 5)
    instance.BackgroundTransparency = bgTransparency or 0.1
    instance.BorderSizePixel = 1
    instance.BorderColor3 = Color3.fromRGB(255, 255, 255)
end

-- [[ 1. SCREEN KEY (MONOCHROME CYBER SYSTEM) ]] --
local KeyFrame = Instance.new("Frame")
KeyFrame.Size = UDim2.new(0, 320, 0, 180)
KeyFrame.Position = UDim2.new(0.5, -160, 0.5, -90)
applyCyberStyle(KeyFrame, 0.05)
KeyFrame.Parent = ScreenGui

local KeyTitle = Instance.new("TextLabel")
KeyTitle.Size = UDim2.new(1, 0, 0, 40)
KeyTitle.Text = "ACCESS REQUIRED // PROTOCOL_7"
KeyTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
KeyTitle.Font = Enum.Font.Code
KeyTitle.TextSize = 15
KeyTitle.BackgroundTransparency = 1
KeyTitle.Parent = KeyFrame

local KeyInput = Instance.new("TextBox")
KeyInput.Size = UDim2.new(0.8, 0, 0, 35)
KeyInput.Position = UDim2.new(0.1, 0, 0.35, 0)
KeyInput.PlaceholderText = "DECRYPT ACCESS KEY..."
KeyInput.Text = ""
KeyInput.TextColor3 = Color3.fromRGB(255, 255, 255)
KeyInput.PlaceholderColor3 = Color3.fromRGB(120, 120, 120)
KeyInput.Font = Enum.Font.Code
KeyInput.TextSize = 14
applyCyberStyle(KeyInput, 0.4)
KeyInput.Parent = KeyFrame

local KeySubmit = Instance.new("TextButton")
KeySubmit.Size = UDim2.new(0.8, 0, 0, 35)
KeySubmit.Position = UDim2.new(0.1, 0, 0.65, 0)
KeySubmit.Text = "[ AUTHORIZE ]"
KeySubmit.TextColor3 = Color3.fromRGB(255, 255, 255)
KeySubmit.Font = Enum.Font.Code
KeySubmit.TextSize = 14
applyCyberStyle(KeySubmit, 0.2)
KeySubmit.Parent = KeyFrame

-- [[ 2. MAIN PANEL (HIGH-CYBER DESIGN) ]] --
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 350, 0, 260)
MainFrame.Position = UDim2.new(0.5, -175, 0.5, -130)
applyCyberStyle(MainFrame, 0.15)
MainFrame.Visible = false
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

-- Ultra-Cyber Background Matrix Clock (Desain Jam Siber Lebih Kompleks)
local ClockContainer = Instance.new("Frame")
ClockContainer.Size = UDim2.new(0, 150, 0, 150)
ClockContainer.Position = UDim2.new(0.5, -75, 0.5, -50)
ClockContainer.BackgroundTransparency = 1
ClockContainer.ZIndex = 0
ClockContainer.Parent = MainFrame

-- Ring Luar Jam Siber
local OuterRing = Instance.new("Frame")
OuterRing.Size = UDim2.new(1, 0, 1, 0)
OuterRing.BackgroundTransparency = 1
OuterRing.BorderSizePixel = 1
OuterRing.BorderColor3 = Color3.fromRGB(255, 255, 255)
OuterRing.Parent = ClockContainer

local ClockLine = Instance.new("Frame")
ClockLine.Size = UDim2.new(0, 2, 0, 65)
ClockLine.Position = UDim2.new(0.5, -1, 0.5, -65)
ClockLine.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
ClockLine.BorderSizePixel = 0
ClockLine.Parent = OuterRing

-- Grid Ornamen Tambahan untuk Kesan Lebih Siber
for i = 1, 4 do
    local DecorLines = Instance.new("Frame")
    DecorLines.Size = UDim2.new(0, 10, 0, 1)
    DecorLines.Position = UDim2.new(0.5, -5, i*0.2, 0)
    DecorLines.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    DecorLines.BackgroundTransparency = 0.5
    DecorLines.BorderSizePixel = 0
    DecorLines.Parent = ClockContainer
end

RunService.RenderStepped:Connect(function(dt)
    if MainFrame.Visible then
        OuterRing.Rotation = (OuterRing.Rotation + (dt * 1500)) % 360 -- Rotasi super cepat siber
    end
end)

local MainTitle = Instance.new("TextLabel")
MainTitle.Size = UDim2.new(1, 0, 0, 35)
MainTitle.Text = "// RCAHUB // CORE_V4 //"
MainTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
MainTitle.Font = Enum.Font.Code
MainTitle.TextSize = 15
MainTitle.BackgroundTransparency = 1
MainTitle.Parent = MainFrame

-- Floating Toggle Button (Open/Close Menu) - Dioptimasi Agar Selalu Muncul
local ToggleButton = Instance.new("TextButton")
ToggleButton.Size = UDim2.new(0, 70, 0, 30)
ToggleButton.Position = UDim2.new(0.05, 0, 0.1, 0)
ToggleButton.Text = "[ MENU ]"
ToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleButton.Font = Enum.Font.Code
ToggleButton.TextSize = 12
applyCyberStyle(ToggleButton, 0.3)
ToggleButton.Visible = false
ToggleButton.Parent = ScreenGui

ToggleButton.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
end)

local function createToggle(name, position, callback)
    local Button = Instance.new("TextButton")
    Button.Size = UDim2.new(0.85, 0, 0, 35)
    Button.Position = position
    Button.Text = name .. " : [ STATUS_OFF ]"
    Button.TextColor3 = Color3.fromRGB(130, 130, 130)
    Button.Font = Enum.Font.Code
    Button.TextSize = 13
    applyCyberStyle(Button, 0.4)
    Button.Parent = MainFrame

    local state = false
    Button.MouseButton1Click:Connect(function()
        state = not state
        if state then
            Button.Text = name .. " : [ STATUS_ON ]"
            Button.TextColor3 = Color3.fromRGB(255, 255, 255)
        else
            Button.Text = name .. " : [ STATUS_OFF ]"
            Button.TextColor3 = Color3.fromRGB(130, 130, 130)
        end
        callback(state)
    end)
end

-- Menyusun Menu Sesuai Permintaan Fitur Baru
createToggle("REAL HEAL AURA", UDim2.new(0.075, 0, 0.22, 0), function(val) Features.HealAura = val end)
createToggle("DETEK KILLER", UDim2.new(0.075, 0, 0.44, 0), function(val) Features.DetectKiller = val end)
createToggle("AIMLOCK (ANTI-STUCK)", UDim2.new(0.075, 0, 0.66, 0), function(val) Features.SilentAim = val end)

-- Key Validasi
KeySubmit.MouseButton1Click:Connect(function()
    if KeyInput.Text == CORRECT_KEY then
        KeyFrame:Destroy()
        MainFrame.Visible = true
        ToggleButton.Visible = true
    else
        KeyInput.Text = ""
        KeyInput.PlaceholderText = "DENIED_SYS_KEY"
    end
end)


-- [[ 3. ENGINE MECHANICS & CODES ]] --

-- Global Core untuk Mendeteksi Target Killer Terdekat
local function getKillerInstance()
    local target = nil
    local minDist = math.huge
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
            local isKiller = false
            -- Pendekatan deteksi berlapis khusus Violence District
            if player.Character:FindFirstChildOfClass("Tool") or (player:FindFirstChild("Role") and player.Role.Value == "Killer") then
                isKiller = true
            elseif player.Character:FindFirstChild("Head") and player.Character.Head:FindFirstChild("Nametag") then
                isKiller = true
            end
            
            if isKiller and player.Character:FindFirstChildOfClass("Humanoid") and player.Character:FindFirstChildOfClass("Humanoid").Health > 0 then
                if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                    local dist = (LocalPlayer.Character.HumanoidRootPart.Position - player.Character.HumanoidRootPart.Position).Magnitude
                    if dist < minDist then
                        minDist = dist
                        target = player
                    end
                end
            end
        end
    end
    return target, minDist
end

-- DETEK KILLER WITH BILLBOARD GUI (! RED / ✅ GREEN)
local IndicatorGui = Instance.new("BillboardGui")
IndicatorGui.Size = UDim2.new(0, 50, 0, 50)
IndicatorGui.AlwaysOnTop = true
IndicatorGui.ExtentsOffset = Vector3.new(0, 3, 0)

local IndicatorLabel = Instance.new("TextLabel")
IndicatorLabel.Size = UDim2.new(1, 0, 1, 0)
IndicatorLabel.BackgroundTransparency = 1
IndicatorLabel.Font = Enum.Font.Code
IndicatorLabel.TextSize = 32
IndicatorLabel.Parent = IndicatorGui

task.spawn(function()
    while task.wait(0.1) do
        local killerPlayer, currentDistance = getKillerInstance()
        
        if Features.DetectKiller and killerPlayer and killerPlayer.Character and killerPlayer.Character:FindFirstChild("Head") then
            IndicatorGui.Parent = killerPlayer.Character.Head
            
            -- Logika Perubahan Tanda Berdasarkan Jarak Jangkauan (30 Meter)
            if currentDistance <= 30 then
                IndicatorLabel.Text = "!"
                IndicatorLabel.TextColor3 = Color3.fromRGB(255, 0, 0) -- Merah jika di bawah 30 meter
            else
                IndicatorLabel.Text = "✅"
                IndicatorLabel.TextColor3 = Color3.fromRGB(0, 255, 0) -- Hijau jika di atas 30 meter
            end
        else
            IndicatorGui.Parent = nil
        end
    end
end)

-- REAL HEAL AURA ENGINE
task.spawn(function()
    while task.wait(0.1) do
        if Features.HealAura and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
            local myHrp = LocalPlayer.Character.HumanoidRootPart
            local myHumanoid = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
            
            -- Menjalankan Bypass Trigger Medis secara real
            if myHumanoid and myHumanoid.Health < myHumanoid.MaxHealth then
                pcall(function()
                    for _, v in pairs(ReplicatedStorage:GetDescendants()) do
                        if v:IsA("RemoteEvent") and (v.Name:lower():find("heal") or v.Name:lower():find("med")) then
                            v:FireServer()
                        end
                    end
                end)
            end
            
            -- Auto-Revive Teman Terdekat
            for _, player in ipairs(Players:GetPlayers()) do
                if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                    if (myHrp.Position - player.Character.HumanoidRootPart.Position).Magnitude <= 15 then
                        for _, prompt in pairs(player.Character:GetDescendants()) do
                            if prompt:IsA("ProximityPrompt") then
                                fireproximityprompt(prompt)
                            end
                        end
                    end
                end
            end
            
            -- VFX Ungu di Lantai Karakter Sesuai Video Referensi
            local p = Instance.new("Part")
            p.Size = Vector3.new(2, 0.1, 2)
            p.CFrame = myHrp.CFrame * CFrame.new(0, -2.4, 0)
            p.Anchored = true
            p.CanCollide = false
            p.Color = Color3.fromRGB(180, 0, 255)
            p.Material = Enum.Material.Neon
            p.Transparency = 0.4
            p.Parent = workspace
            
            local m = Instance.new("SpecialMesh")
            m.MeshType = Enum.MeshType.Cylinder
            m.Parent = p
            
            TweenService:Create(p, TweenInfo.new(0.2), {Size = Vector3.new(6.5, 0.05, 6.5), Transparency = 1}):Play()
            game:GetService("Debris"):AddItem(p, 0.2)
        end
    end
end)

-- AIMLOCK (ANTI-STUCK OPTIMIZED FOR MOBILE EXECUTORS)
-- Menghilangkan ketergantungan metatable raycast agar kamera analog HP tetap bergerak bebas
RunService.RenderStepped:Connect(function()
    if Features.SilentAim and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
        local killerTarget, _ = getKillerInstance()
        if killerTarget and killerTarget.Character and killerTarget.Character:FindFirstChild("HumanoidRootPart") then
            local targetHrp = killerTarget.Character.HumanoidRootPart
            
            -- Memeriksa apakah player menekan tombol serang (Karakter memegang Tool aktif)
            local activeTool = LocalPlayer.Character:FindFirstChildOfClass("Tool")
            if activeTool then
                -- Memanipulasi CFrame tubuh/serangan langsung menghadap Killer secara real-time tanpa mengunci paksa sumbu rotasi kamera utama
                local targetPosition = Vector3.new(targetHrp.Position.X, LocalPlayer.Character.HumanoidRootPart.Position.Y, targetHrp.Position.Z)
                LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(LocalPlayer.Character.HumanoidRootPart.Position, targetPosition)
            end
        end
    end
end)
