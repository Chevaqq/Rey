-- [[ REYHUB / ACCOORNNHUB - VIOLENCE DISTRICT V9 (PERFECT VIDEO REPLICA) ]] --

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

-- State Global
local CurrentTarget = nil
local IsHoldingSpear = false

-- Konfigurasi Fitur
local Config = {
    ChamsSurvivor = true,
    SilentAimVeil = true,
    AimbotSmoothness = 0.18 -- Kecepatan tarikan kamera halus seperti di video
}

-- [[ UI SYSTEM SIMPLE (TRANSPARAN & OPEN/CLOSE) ]] --
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "ReyHub_ViolenceDistrict_V9"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Size = UDim2.new(0, 90, 0, 35)
ToggleBtn.Position = UDim2.new(0, 10, 0, 10)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
ToggleBtn.BackgroundTransparency = 0.3
ToggleBtn.TextColor3 = Color3.fromRGB(0, 255, 255)
ToggleBtn.Text = "CLOSE"
ToggleBtn.TextSize = 14
ToggleBtn.Font = Enum.Font.SourceSansBold
ToggleBtn.Parent = ScreenGui

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 300, 0, 150)
MainFrame.Position = UDim2.new(0.5, -150, 0.5, -75)
MainFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
MainFrame.BackgroundTransparency = 0.5
MainFrame.BorderSizePixel = 2
MainFrame.BorderColor3 = Color3.fromRGB(0, 255, 255)
MainFrame.Parent = ScreenGui

ToggleBtn.MouseButton1Click:Connect(function()
    if MainFrame.Visible then
        MainFrame.Visible = false
        ToggleBtn.Text = "OPEN"
    else
        MainFrame.Visible = true
        ToggleBtn.Text = "CLOSE"
    end
end)

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 35)
Title.Text = "VEIL SILENT AIM (VIDEO VIBES)"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Font = Enum.Font.SourceSansBold
Title.TextSize = 16
Title.BackgroundTransparency = 1
Title.Parent = MainFrame

local function CreateToggle(name, pos, configKey)
    local Btn = Instance.new("TextButton")
    Btn.Size = UDim2.new(0, 240, 0, 35)
    Btn.Position = pos
    Btn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    Btn.Text = name .. " : ON"
    Btn.TextColor3 = Color3.fromRGB(50, 255, 50)
    Btn.Font = Enum.Font.SourceSansBold
    Btn.TextSize = 14
    Btn.Parent = MainFrame

    Btn.MouseButton1Click:Connect(function()
        Config[configKey] = not Config[configKey]
        if Config[configKey] then
            Btn.Text = name .. " : ON"
            Btn.TextColor3 = Color3.fromRGB(50, 255, 50)
        else
            Btn.Text = name .. " : OFF"
            Btn.TextColor3 = Color3.fromRGB(255, 50, 50)
        end
    end)
end

CreateToggle("CHAMS SURVIVOR (GREEN)", UDim2.new(0.5, -120, 0.35, 0), "ChamsSurvivor")
CreateToggle("SILENT VEIL (AUTO LOCK)", UDim2.new(0.5, -120, 0.65, 0), "SilentAimVeil")


-- [[ UTILITIES: DETEKSI ROLE & CEK TOMBAK ]] --
local function IsSurvivor(player)
    if player ~= LocalPlayer and player:GetAttribute("Role") ~= "Killer" and not player.Name:lower():find("killer") then
        return true
    end
    return false
end

local function CheckIfHoldingSpear()
    local Char = LocalPlayer.Character
    if Char then
        -- Mengecek apakah ada objek Tombak/Weapon di dalam karakter (artinya sedang dipegang)
        for _, tool in pairs(Char:GetChildren()) do
            if tool:IsA("Tool") or tool.Name:lower():find("tombak") or tool.Name:lower():find("spear") or tool.Name:lower():find("weapon") then
                return true
            end
        end
    end
    return false
end

local function GetClosestSurvivor()
    local Target = nil
    local ShortestDistance = math.huge

    for _, v in pairs(Players:GetPlayers()) do
        if IsSurvivor(v) and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
            local Humanoid = v.Character:FindFirstChildOfClass("Humanoid")
            if Humanoid and Humanoid.Health > 0 then
                local ScreenPos, OnScreen = Camera:WorldToViewportPoint(v.Character.HumanoidRootPart.Position)
                if OnScreen then
                    local MousePos = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
                    local Mag = (Vector2.new(ScreenPos.X, ScreenPos.Y) - MousePos).Magnitude
                    if Mag < ShortestDistance then
                        ShortestDistance = Mag
                        Target = v
                    end
                end
            end
        end
    end
    return Target
end


-- [[ RENDERING LOOP ENGINE ]] --
RunService.RenderStepped:Connect(function()
    CurrentTarget = GetClosestSurvivor()
    IsHoldingSpear = CheckIfHoldingSpear()

    -- 1. CHAMS SURVIVOR (HIJAU TRANSURAN)
    for _, p in pairs(Players:GetPlayers()) do
        if IsSurvivor(p) and p.Character then
            local Highlight = p.Character:FindFirstChildOfClass("Highlight")
            if Config.ChamsSurvivor then
                if not Highlight then
                    Highlight = Instance.new("Highlight", p.Character)
                end
                Highlight.FillColor = Color3.fromRGB(0, 255, 0)
                Highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
                Highlight.FillTransparency = 0.5
                Highlight.OutlineTransparency = 0
                Highlight.Enabled = true
            else
                if Highlight then Highlight:Destroy() end
            end
        end
    end

    -- 2. DYNAMIC CAMERA LOCK (Hanya Mengunci Saat Pegang Tombak)
    if Config.SilentAimVeil and IsHoldingSpear and CurrentTarget and CurrentTarget.Character and CurrentTarget.Character:FindFirstChild("HumanoidRootPart") then
        local TargetRoot = CurrentTarget.Character.HumanoidRootPart
        
        -- Kalkulasi prediksi lari target agar lemparan presisi
        local TargetVelocity = TargetRoot.AssemblyLinearVelocity
        local Distance = (TargetRoot.Position - Camera.CFrame.Position).Magnitude
        local TimeToHit = Distance / 900
        local PredictedPos = TargetRoot.Position + (TargetVelocity * TimeToHit) + Vector3.new(0, 0.2, 0)

        -- Kamera mengikuti target secara halus (Lerp) hanya jika tombak dipegang
        -- Jari kamu tetap bisa mengontrol arah geser sedikit (tidak nge-stuck kaku)
        local TargetCFrame = CFrame.lookAt(Camera.CFrame.Position, PredictedPos)
        Camera.CFrame = Camera.CFrame:Lerp(TargetCFrame, Config.AimbotSmoothness)
    end
end)


-- [[ METAMETHOD HOOK FOR PERFECT HIT TOMBAK ]] --
local OldNamecall
OldNamecall = hookmetamethod(game, "__namecall", function(Self, ...)
    local Args = {...}
    local Method = getnamecallmethod()

    if Config.SilentAimVeil and IsHoldingSpear and (Method == "Raycast" or Method == "FindPartOnRayWithIgnoreList") then
        if CurrentTarget and CurrentTarget.Character and CurrentTarget.Character:FindFirstChild("HumanoidRootPart") then
            local TargetRoot = CurrentTarget.Character.HumanoidRootPart
            
            -- Memaksa garis tembakan/lemparan server lurus 100% ke arah target
            if Method == "Raycast" and typeof(Args[1]) == "Vector3" and typeof(Args[2]) == "Vector3" then
                Args[2] = (TargetRoot.Position - Args[1]).Unit * 9999
            elseif Args[1] and typeof(Args[1]) == "Ray" then
                Args[1] = Ray.new(Args[1].Origin, (TargetRoot.Position - Args[1].Origin).Unit * 9999)
            end
        end
    end
    return OldNamecall(Self, unpack(Args))
end)
