-- =================================================================
-- VIOLENCE DISTRICT - CYBERPUNK SILENT AIM SCRIPT
-- RUN WITH DELTA EXECUTOR
-- =================================================================

local UserInputService = game:GetService("UserInputService")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

-- --- CONFIGURATION & STATES ---
local ScriptConfig = {
    SilentAimEnabled = true,
    TargetTeamName = "Killers", -- Ganti sesuai nama tim Killer di game jika ada sistem tim resmi
    CyberColor = Color3.fromRGB(0, 180, 255),
    DarkBgColor = Color3.fromRGB(10, 16, 26)
}

-- --- CUSTOM TARGET LOOKUP FOR KILLER ---
-- Fungsi khusus untuk mendeteksi Killer berdasarkan item atau kondisi di Violence District
local function IsKiller(player)
    if player == LocalPlayer then return false end
    
    -- 1. Berdasarkan Tim (Jika game memakai sistem Team)
    if player.Team and (player.Team.Name:lower():find("killer") or player.Team.Name:lower():find("slasher")) then
        return true
    end
    
    -- 2. Berdasarkan Tool/Senjata yang dipegang (e.g. Pisau/Gergaji)
    local character = player.Character
    if character then
        for _, item in pairs(character:GetChildren()) do
            if item:IsA("Tool") and (item.Name:lower():find("knife") or item.Name:lower():find("weapon") or item.Name:lower():find("murd")) then
                return true
            end
        end
    end
    
    return false
end

-- --- SILENT AIM CORE ENGINE ---
-- Mendapatkan target Killer terdekat yang posisinya valid
local function GetClosestKiller()
    local closestTarget = nil
    local shortestDistance = math.huge

    for _, player in pairs(Players:GetPlayers()) do
        if IsKiller(player) then
            local character = player.Character
            local rootPart = character and character:FindFirstChild("HumanoidRootPart")
            local humanoid = character and character:FindFirstChildOfClass("Humanoid")
            
            if rootPart and humanoid and humanoid.Health > 0 then
                -- Hitung jarak antara karakter kita dan Killer
                local myRoot = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
                if myRoot then
                    local distance = (rootPart.Position - myRoot.Position).Magnitude
                    if distance < shortestDistance then
                        shortestDistance = distance
                        closestTarget = rootPart
                    end
                end
            end
        end
    end
    return closestTarget
end

-- Hooking system tembakan via Namecall (Metamethod Hooking)
-- Tetap mendeteksi target meskipun peluru diarahkan ke tempat lain (tembak angin)
local gmt = getrawmetatable(game)
setreadonly(gmt, false)
local oldNamecall = gmt.__namecall

gmt.__namecall = newcclosure(function(self, ...)
    local method = getnamecallmethod()
    local args = {...}
    
    if ScriptConfig.SilentAimEnabled and (method == "FindPartOnRayWithIgnoreList" or method == "FindPartOnRay" or method == "Raycast") then
        local targetPart = GetClosestKiller()
        if targetPart then
            -- Membelokkan arah vector peluru langsung menuju posisi kepala/badan Killer target
            if method == "Raycast" then
                local origin = args[1]
                args[2] = (targetPart.Position - origin).Unit * 1000
            else
                local origin = args[1].Origin
                args[1] = Ray.new(origin, (targetPart.Position - origin).Unit * 1000)
            end
            return oldNamecall(self, unpack(args))
        end
    end
    return oldNamecall(self, ...)
end)
setreadonly(gmt, true)


-- =================================================================
-- MODERN CYBERPUNK UI GENERATOR (DARK BLUE WITH ANALOG CLOCK)
-- =================================================================

-- Destroy GUI lama jika sengaja di-execute ulang
if game.CoreGui:FindFirstChild("CyberHub_ViolenceDistrict") then
    game.CoreGui["CyberHub_ViolenceDistrict"]:Destroy()
end

-- --- SCREEN GUI ---
local CyberGui = Instance.new("ScreenGui")
CyberGui.Name = "CyberHub_ViolenceDistrict"
CyberGui.Parent = game.CoreGui
CyberGui.ResetOnSpawn = false

-- --- MAIN PANEL ---
local MainPanel = Instance.new("Frame")
MainPanel.Name = "MainPanel"
MainPanel.Size = UDim2.new(0, 360, 0, 240)
MainPanel.Position = UDim2.new(0.5, -180, 0.4, -120)
MainPanel.BackgroundColor3 = ScriptConfig.DarkBgColor
MainPanel.BackgroundTransparency = 0.15
MainPanel.BorderSizePixel = 0
MainPanel.Active = true
MainPanel.Draggable = true
MainPanel.Parent = CyberGui

-- Cyber Border Glow Effect
local UIStroke = Instance.new("UIStroke")
UIStroke.Color = ScriptConfig.CyberColor
UIStroke.Thickness = 2
UIStroke.Parent = MainPanel

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 8)
UICorner.Parent = MainPanel

-- --- TITLE BAR ---
local Title = Instance.new("TextLabel")
Title.Name = "Title"
Title.Size = UDim2.new(1, -20, 0, 35)
Title.Position = UDim2.new(0, 15, 0, 5)
Title.BackgroundTransparency = 1
Title.Text = "CYBER HUB // DISTR_VIOLENCE"
Title.TextColor3 = ScriptConfig.CyberColor
Title.TextSize = 16
Title.Font = Enum.Font.Code
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = MainPanel

-- --- ANALOG CYBER CLOCK BACKGROUND ---
local ClockFrame = Instance.new("Frame")
ClockFrame.Name = "AnalogClock"
ClockFrame.Size = UDim2.new(0, 110, 0, 110)
ClockFrame.Position = UDim2.new(1, -130, 0.5, -45)
ClockFrame.BackgroundColor3 = Color3.fromRGB(20, 30, 50)
ClockFrame.BackgroundTransparency = 0.6
ClockFrame.BorderSizePixel = 0
ClockFrame.Parent = MainPanel

local ClockCorner = Instance.new("UICorner")
ClockCorner.CornerRadius = UDim.new(1, 0) -- Circle shape
ClockCorner.Parent = ClockFrame

local ClockStroke = Instance.new("UIStroke")
ClockStroke.Color = ScriptConfig.CyberColor
ClockStroke.Thickness = 1
ClockStroke.Parent = ClockFrame

-- Center Dot for Clock
local CenterDot = Instance.new("Frame")
CenterDot.Size = UDim2.new(0, 6, 0, 6)
CenterDot.Position = UDim2.new(0.5, -3, 0.5, -3)
CenterDot.BackgroundColor3 = ScriptConfig.CyberColor
CenterDot.BorderSizePixel = 0
CenterDot.Parent = ClockFrame

-- Generate Numbers (12, 3, 6, 9) inside Cyber Clock
local clockNumbers = {["12"] = UDim2.new(0.5, -10, 0, 5), ["3"] = UDim2.new(1, -15, 0.5, -7), ["6"] = UDim2.new(0.5, -10, 1, -20), ["9"] = UDim2.new(0, 5, 0.5, -7)}
for numStr, pos in pairs(clockNumbers) do
    local numLabel = Instance.new("TextLabel")
    numLabel.Size = UDim2.new(0, 20, 0, 15)
    numLabel.Position = pos
    numLabel.BackgroundTransparency = 1
    numLabel.Text = numStr
    numLabel.TextColor3 = ScriptConfig.CyberColor
    numLabel.TextSize = 10
    numLabel.Font = Enum.Font.Code
    numLabel.Parent = ClockFrame
end

-- Clock Hands (Jarum Jam & Menit)
local HourHand = Instance.new("Frame")
HourHand.Size = UDim2.new(0, 2, 0, 25)
HourHand.AnchorPoint = Vector2.new(0.5, 1)
HourHand.Position = UDim2.new(0.5, 0, 0.5, 0)
HourHand.BackgroundColor3 = ScriptConfig.CyberColor
HourHand.BorderSizePixel = 0
HourHand.Parent = ClockFrame

local MinuteHand = Instance.new("Frame")
MinuteHand.Size = UDim2.new(0, 1.5, 0, 40)
MinuteHand.AnchorPoint = Vector2.new(0.5, 1)
MinuteHand.Position = UDim2.new(0.5, 0, 0.5, 0)
MinuteHand.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
MinuteHand.BorderSizePixel = 0
MinuteHand.Parent = ClockFrame

-- Thread Logic untuk Update Jarum Jam Real-time
spawn(function()
    while task.wait(1) do
        local date = os.date("*t")
        local hourAngle = (date.hour % 12) * 30 + date.min * 0.5
        local minAngle = date.min * 6
        HourHand.Rotation = hourAngle
        MinuteHand.Rotation = minAngle
    end
end)


-- --- TOGGLE BUTTON (SILENT AIM LOCK KILLER) ---
local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Name = "ToggleSilentAim"
ToggleBtn.Size = UDim2.new(0, 180, 0, 40)
ToggleBtn.Position = UDim2.new(0, 15, 0, 65)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(0, 50, 80)
ToggleBtn.Font = Enum.Font.Code
ToggleBtn.Text = "SILENT AIM: ON"
ToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleBtn.TextSize = 14
ToggleBtn.Parent = MainPanel

local BtnCorner = Instance.new("UICorner")
BtnCorner.CornerRadius = UDim.new(0, 4)
BtnCorner.Parent = ToggleBtn

local BtnStroke = Instance.new("UIStroke")
BtnStroke.Color = ScriptConfig.CyberColor
BtnStroke.Thickness = 1
BtnStroke.Parent = ToggleBtn

ToggleBtn.MouseButton1Click:Connect(function()
    ScriptConfig.SilentAimEnabled = not ScriptConfig.SilentAimEnabled
    if ScriptConfig.SilentAimEnabled then
        ToggleBtn.Text = "SILENT AIM: ON"
        ToggleBtn.BackgroundColor3 = Color3.fromRGB(0, 50, 80)
        ToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    else
        ToggleBtn.Text = "SILENT AIM: OFF"
        ToggleBtn.BackgroundColor3 = Color3.fromRGB(40, 20, 20)
        ToggleBtn.TextColor3 = Color3.fromRGB(150, 150, 150)
    end
end)

-- --- WATERMARK / INFO TEXT ---
local InfoText = Instance.new("TextLabel")
InfoText.Size = UDim2.new(0, 180, 0, 50)
InfoText.Position = UDim2.new(0, 15, 0, 120)
InfoText.BackgroundTransparency = 1
InfoText.Text = "Target Lock: Killer Only\nStatus: Secure [Delta]"
InfoText.TextColor3 = Color3.fromRGB(120, 150, 180)
InfoText.TextSize = 12
InfoText.Font = Enum.Font.Code
InfoText.TextXAlignment = Enum.TextXAlignment.Left
InfoText.Parent = MainPanel


-- --- FLOATING OPEN / CLOSE BUTTON ---
local OpenCloseBtn = Instance.new("TextButton")
OpenCloseBtn.Name = "CyberToggleMenu"
OpenCloseBtn.Size = UDim2.new(0, 60, 0, 35)
OpenCloseBtn.Position = UDim2.new(0.1, 0, 0.05, 0) -- Posisinya pas di layar HP agar mudah dijangkau
OpenCloseBtn.BackgroundColor3 = ScriptConfig.DarkBgColor
OpenCloseBtn.Font = Enum.Font.Code
OpenCloseBtn.Text = "MENU"
OpenCloseBtn.TextColor3 = ScriptConfig.CyberColor
OpenCloseBtn.TextSize = 12
OpenCloseBtn.Active = true
OpenCloseBtn.Draggable = true -- Bisa digeser sesuka hati di HP
OpenCloseBtn.Parent = CyberGui

local ObjCorner = Instance.new("UICorner")
ObjCorner.CornerRadius = UDim.new(0, 6)
ObjCorner.Parent = OpenCloseBtn

local ObjStroke = Instance.new("UIStroke")
ObjStroke.Color = ScriptConfig.CyberColor
ObjStroke.Thickness = 1.5
ObjStroke.Parent = OpenCloseBtn

-- Mekanisme Minimalkan UI saat tombol di klik
local MenuVisible = true
OpenCloseBtn.MouseButton1Click:Connect(function()
    MenuVisible = not MenuVisible
    MainPanel.Visible = MenuVisible
    if MenuVisible then
        OpenCloseBtn.TextColor3 = ScriptConfig.CyberColor
    else
        OpenCloseBtn.TextColor3 = Color3.fromRGB(100, 100, 100)
    end
end)
